# CSS Warnings

## Vendor Prefixes
- `backdrop-filter` needs `-webkit-backdrop-filter` for Safari
- Flexbox `gap` property has partial support in older browsers

## Notes
- Prefer inline styles for single-file output compatibility
- Use `box-sizing: border-box` globally
- SVG elements don't inherit font styles — set explicitly
